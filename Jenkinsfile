// Jenkinsfile
pipeline {
    agent any
    tools {
        maven 'mymaven'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Hello, Jenkins!'
                // For Windows, you can use bat 'echo Hello, Jenkins!'
            }
        }
    }
}